jQuery(document).ready(function($) {
	
	// if on the widgets page, hide all of the voce post widget areas
	if ( $('body.widgets-php').length ) {
		//$('[id^="voce_post_meta_widget_area_"]').closest(".widgets-holder-wrap").hide();
	} else {

	}
});
